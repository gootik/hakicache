{erl_opts, [
    debug_info,
    {parse_transform, lager_transform}
]}.

{deps, [
    {lager, "3.5.1"}
]}.

{profiles, [
    {debug, [
        {erl_opts, [
            {d, 'DEBUG'}
        ]}
    ]}
]}.